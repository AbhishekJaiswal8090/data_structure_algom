#include <iostream>
using namespace std;

// From now on we are going to study linked list
// The linked list is a linear data structure made up of nodes
// where each node contains data and pointer to the next node
// compare to arrays they are more flexible in insertion and deletion
// but traversal and accessing elements takes time unlike arrays you can just access by indexes

// Node => (data and pointer)
// moreover the arrays are contigous but linked list aren't they are connected my pointers
// the nodes could be anywhere in memory but connected by pointer

// The first node is point by a pointer called Head and same last node is point by last pointer Tail

int main()
{
}